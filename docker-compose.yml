version: '3.8'

services:
  server:
    build: ./api
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - /app/node_modules
      - ./api:/app
  client:
    build: ./client
    restart: always
    depends_on:
      - server
    ports:
      - "8080:80"
    volumes:
      - /app/node_modules
      - ./client:/app
    
    